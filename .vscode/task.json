{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake: build",
            "type": "shell",
            "command": "cmake --build build --config Debug -- -j",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
